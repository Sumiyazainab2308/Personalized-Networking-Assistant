# ─────────────────────────────────────────────────────────────────────────────
# Personalized Networking Assistant — Python Dependencies
# Python 3.11+
# ─────────────────────────────────────────────────────────────────────────────

# ── FastAPI & ASGI ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0                  # async HTTP client (tests + health checks)

# ── Pydantic / Settings ────────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.2.1

# ── Hugging Face Transformers & Inference ─────────────────────────────────────
transformers>=4.40.0
torch>=2.2.0                   # CPU-only by default; GPU optional
sentencepiece>=0.1.99          # required by BART tokenizer

# ── Wikipedia API ─────────────────────────────────────────────────────────────
Wikipedia-API>=0.6.0

# ── Streamlit Frontend ────────────────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.2.0
requests>=2.31.0               # HTTP calls from Streamlit to FastAPI

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
anyio>=4.3.0                   # async backend for pytest-asyncio

# ── Development & Linting (optional) ─────────────────────────────────────────
# ruff>=0.4.0
# mypy>=1.10.0
